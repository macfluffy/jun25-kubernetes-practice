apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-deployment
spec:
  selector:
    matchLabels:
      app: demo-deployment
    replicas: 2
    template:
      metadata:
        labels:
          app: demo-deployment
        
      spec:
        containers:
          - name: 